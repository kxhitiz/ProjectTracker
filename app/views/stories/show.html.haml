%h2
  = @story.title
= @story.description
%br
= @story.status
.story_status_change
  = render "status_change_form"
.assign_story
  = "Currently assigned to "
  - if @story.person_id
    = getName(@story.person_id)
  - else
    = "none"
  = render "assign_story_form"

